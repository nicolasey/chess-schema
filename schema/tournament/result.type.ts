/**
 * Represents the result one Player had playing a Match
 * Caution: points might differ depending on tournament
 */
export enum Result {
  LOST = "-",
  WIN = "+",
  DRAW = "=",
  ADJOURNED = "?",
}
